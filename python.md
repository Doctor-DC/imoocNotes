####变量名命名规范：- 变量名区分大小；- 变量名必须是以字母或下划线字符开头，不能为数字开头；- 变量名中不能包含空格；####"""三重引号字符- 长字符串：若希望得到一个跨多行的字符串，必须使用一种特殊的字符串，称为三重引号字符。####python2 & 3 中除法区别。- 在Python2中，/是做整除- 在Python3中，/是做常规除法，//是做整除####** & %：- 2**3 = 8即2的三次方```python3 % 2 = 11 % 2 = 1```####Python中的操作符：- `+` `-` `*` `/` 操作符- `=` 赋值操作符操作的东西是操作数####e:10的几次方- 3e5 = 3*10^5- 1.752e-13 = 1.752*10^-13####类型转换：- float()- int()- str()####type()查看变量的类型####raw_input()```pythonsomename = raw_input()```####print 逗号。```pythonprint a,b```使用逗号将多个print语句合并到同一行时，Python输出结果会自动增加一个空格。```pythonprint "enter you name:",somename =raw_input() #同一行somename = raw_input( "enter your name："  )#两行：print  “ enter your name: ”somename =raw_input() ```####python 2 & 3 - Python3中使用print()时，要打印的内容必须在括号中。- 在Python3中，在行尾添加逗号打印内容在同一行的方法不再有效。- 在Python3中，raw_input()改名为input()，   - 与Python2中的raw_input()完全一样。####获取互联网输入```pythonimport urllibfile = urllib.urlopen('http://www.baidu.com')message = file.read()print message ```####```python# 对话框userresponse = easygui.msgbox("hello there")```- 制作多个按钮的对话框：```pythonchoice = easygui.buttonbox("ni yao sha?",choices = ['v','i','c']easygui.msgbox("you picked" + choice)```- 选择框得到输入```pythonchoice = easygui.choicebox("ni yao sha?",choices = ['v','i','c']easygui.msgbox("you picked" + choice)```- 文本输入```pythonchoice = easygui.enterbox("ni yao sha?")easygui.msgbox("you picked" + choice)```- 文本输入(默认值输入)```pythonchoice = easygui.enterbox("ni yao sha?",default = "hello")easygui.msgbox("you picked" + choice)```